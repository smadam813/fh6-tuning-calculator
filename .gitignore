# Dependencies — this is a zero-build static app; guard against future tooling
node_modules/

# Logs & debug output
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test / coverage output (node --test --experimental-test-coverage, nyc, etc.)
coverage/
.nyc_output/
*.lcov

# Generated parity snapshot — reproduced from the legacy JS engine by
# `node legacy/parity-export.js` (run automatically by the test project's
# GenerateParityCases MSBuild target). Reproducible from source; not tracked.
parity/cases.json

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# .NET build output
bin/
obj/

# Superpowers planning artifacts (local agent scratch; not part of the project)
docs/superpowers/
